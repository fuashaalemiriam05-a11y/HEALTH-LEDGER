# Database
DB_HOST=localhost
DB_NAME=health_record_system
DB_USER=root
DB_PASS=

# Application
APP_ENV=development

# Encryption (generate with: php -r "echo bin2hex(random_bytes(16));")
ENCRYPTION_KEY=
